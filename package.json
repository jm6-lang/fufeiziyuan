{
  "name": "paid-download-site",
  "version": "1.0.0",
  "description": "付费资源下载站 - 基于 Cloudflare Pages + Workers + R2",
  "private": true,
  "scripts": {
    "deploy:frontend": "echo '前端通过 GitHub 连接到 Cloudflare Pages 自动部署，请 push 到 GitHub'",
    "deploy:worker": "wrangler deploy",
    "dev:worker": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
